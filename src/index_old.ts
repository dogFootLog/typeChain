/*
class Block {
  constructor(private data: string) {}
  static hello() {
    return "hi;";
  }
}
*/

/*
import { init, exit } from "myPackage";

init({ url: "true" });
exit(1);
*/
/*
import { init, exit } from "./myPackage.js";

init({
  url: "true",
  debug: false,
});
exit(1);
*/
console.log("bye");
